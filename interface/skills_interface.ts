export interface SkillCategory {
  category: string;
  svgIcon?: string; // Optional, you can assign an icon URL or class if needed
  description: string;
  icon?: string; // Optional, you can assign an icon URL or class if needed
  skills: string[];
}
